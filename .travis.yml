language: php

php:
  - '7.4'

services:
  - mysql
  - redis-server

env:
  global:
    - CC_TEST_REPORTER_ID=
    - DB_HOSTNAME=127.0.0.1
    - DB_DATABASE=slim4_api_crud_sql
    - DB_USERNAME=root
    - DB_PASSWORD=
    - SECRET_KEY=YourSuperSecret-KeY
    - REDIS_ENABLED=true
    - REDIS_URL=localhost

before_install:
  - mysql -e 'CREATE DATABASE slim4_api_crud_sql;'

before_script:
  - mysql slim4_api_crud_sql < database/database.sql
  - composer install
  - curl -L https://codeclimate.com/downloads/test-reporter/test-reporter-latest-linux-amd64 > ./cc-test-reporter 
  - chmod +x ./cc-test-reporter 
  - ./cc-test-reporter before-build

script:
  - vendor/bin/phpunit --coverage-text --coverage-clover build/logs/clover.xml --whitelist src
  - if [ $(phpenv version-name) = "7.4" ] && [ "$TRAVIS_PULL_REQUEST" == "false" ]; then ./cc-test-reporter after-build --exit-code $TRAVIS_TEST_RESULT; fi
